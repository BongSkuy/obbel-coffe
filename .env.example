# Database
# For production, use PostgreSQL or MySQL instead of SQLite
# SQLite is not recommended for Vercel (serverless environment)

# Option 1: PostgreSQL (Recommended for Vercel)
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# Option 2: MySQL
# DATABASE_URL="mysql://user:password@host:port/database"

# Option 3: SQLite (Development only - NOT recommended for Vercel)
# DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL="https://your-domain.vercel.app"
NEXTAUTH_SECRET="your-secret-key-here"

# Other environment variables if needed
